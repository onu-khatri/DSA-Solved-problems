using ByteByteCoding.TwoPointers.NumberPairs;
using ByteByteCoding.TwoPointers.Palindrome;

// var pairs = PairSumSorted.FindPairs([1, 1, 1, 2, 2, 3], 4);
// Console.WriteLine(String.Join(" | ", pairs.Select(t => string.Join(',', t))));

// var pairs = PairTripletSum.FindPairs([1, 1, 1, 2, 2, 3], 6);
// Console.WriteLine(String.Join(" | ", pairs.Select(t => string.Join(',', t))));

Console.WriteLine($"string is {PalindromeSkipSpecialChar.IsPalindrome("A@@nn$A", ['@', '$'])} Palindrome");